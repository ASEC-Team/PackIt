{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"PackIt","text":"<p>PackIt is a highly flexible, schema-based serialization framework engineered for long-term data persistence and backward compatibility. Designed to sanitize and compress data efficiently, PackIt supports extensible serialization Modes and Engines, allowing developers to build custom implementations tailored to their specific technical requirements.</p>"},{"location":"#performance-compression","title":"Performance &amp; Compression","text":"<p>PackIt is highly optimized for data efficiency, delivering an average 65% reduction in data size compared to standard JSON encoding.</p> <p>When paired with Roblox's native buffer compression it scales even further achieving up to 90% size reduction against JSON.</p> <p>This drastic footprint reduction minimizes bandwidth consumption, making it an ideal choice for both high-frequency network replication and storage-capped data persistence.</p>"},{"location":"#extensibility-community-contributions","title":"Extensibility &amp; Community Contributions","text":"<p>The framework is engineered from the ground up for high extensibility, allowing developers to scale its core capabilities effortlessly. Because the architecture decouples structural design from the underlying serialization logic, the community can easily implement and integrate custom Engines, Modes, and specialized Data Types. This ensures that PackIt can adapt to unique project demands and evolve alongside community-driven innovations.</p>"},{"location":"#core-concepts","title":"Core Concepts","text":""},{"location":"#schema","title":"Schema","text":"<p>A Schema defines the structure and representation of your data. It supports multiple Variants, enabling seamless serialization and deserialization across different structural versions.</p> <ul> <li> <p>Forward-Compatible Writing: By default, PackIt serializes data using the latest available variant.</p> </li> <li> <p>Automated Migration: When deserializing legacy data, the framework automatically migrates older variants to the latest schema version seamlessly.</p> </li> </ul>"},{"location":"#variant","title":"Variant","text":"<p>A Variant represents a specific iteration of a schema, created by extending or overwriting parts of existing data representations to accommodate evolving application requirements.</p>"},{"location":"#engine","title":"Engine","text":"<p>The Engine determines the underlying serialization mechanics, dictating exactly how data is encoded into the buffer and how individual data types are managed. </p> <ul> <li>Abstraction: Engine configurations are entirely self-contained within the serialized payload, meaning you do not need to specify or know the engine variant during deserialization.</li> </ul>"},{"location":"#mode","title":"Mode","text":"<p>Modes instruct engines on how to layout the serialized buffer based on specific performance and mutability requirements:</p> <ul> <li> <p>Flat Mode   Packs all data inline, restricting subsequent structural modifications to the very end of the buffer. This maximizes encoding efficiency and minimizes footprint, making it ideal for high-performance, ephemeral runtime data such as remote network events.</p> </li> <li> <p>Nested Mode   Isolates individual dictionaries with distinct structural boundaries, allocating space for new properties to be appended to any nested object layer. This layout is highly optimized for persistent data stores that continuously evolve over time.</p> </li> </ul>"},{"location":"#fields","title":"Fields","text":"<p>Every data field in your schema pairs a Type Method with an optional Options Object. This gives you granular control over memory boundaries, fallback behaviors, and performance profiling before data ever touches the wire.</p> <p>Fallback Defaults</p> <ul> <li>Automatic Guardrails: If a field is omitted at runtime, the engine seamlessly inserts your configured <code>Default</code> value.</li> <li>Instant Boilerplates: These values are used to mock out perfectly formatted, fresh data structures out of the box.</li> </ul> <p>Sanitization Pipelines</p> <ul> <li>Inline Processing: <code>Sanitizers</code> are sequential arrays of functions executed automatically during serialization, deserialization and versioning changes.</li> <li>Automated Cleanup: They intercept the runtime value along with its layout type string, allowing you to clamp numbers, normalize strings, or validate data on the fly.</li> </ul> <p>Bit Budgets Sizes To maximize compression, numbers and enums are packed into precise bit-allocation boxes. If omitted, types default to their tightest possible memory footprint - BYTE, WORD, DWORD, QWORD.</p>"},{"location":"API/","title":"API","text":""},{"location":"API/#core-module","title":"Core Module","text":""},{"location":"API/#dict","title":"<code>.dict()</code>","text":"<p><pre><code>Packit.dict(tag: string?, ...: string): Schema\n</code></pre> Instantiates a new dictionary-based <code>Schema</code>. </p> <p>Parameters:</p> <ul> <li><code>tag</code> (string, optional): A unique identifier for the schema. If provided, the schema is registered globally and can be retrieved across different scripts using <code>Packit.get(tag)</code>.</li> <li><code>...</code> (string, optional): A list of optional alias names that can also be used to query this schema.</li> </ul> <p>Returns: A <code>Schema</code> object configured for dictionary structures.</p>"},{"location":"API/#list","title":"<code>.list()</code>","text":"<p><pre><code>Packit.list(tag: string?, ...: string): Schema\n</code></pre> Instantiates a new list-based <code>Schema</code>.</p> <p>Parameters:</p> <ul> <li><code>tag</code> (string, optional): A unique identifier for the schema. If provided, the schema is registered globally and can be retrieved across different scripts using <code>Packit.get(tag)</code>.</li> <li><code>...</code> (string, optional): A list of optional alias names that can also be used to query this schema.</li> </ul> <p>Returns: A <code>Schema</code> object configured for array/list structures.</p>"},{"location":"API/#get","title":"<code>.get()</code>","text":"<p><pre><code>Packit.get(tag: string): Schema?\n</code></pre> Retrieves a previously registered schema from the global registry using its defined identifier or alias.</p> <p>Parameters:</p> <ul> <li><code>tag</code> (string): The identifier or alias assigned during the schema's creation.</li> </ul> <p>Returns: The matching <code>Schema</code> object if found; otherwise throws an error.</p>"},{"location":"API/#schema-variant","title":"Schema &amp; Variant","text":"<p>The following methods are valid for both Dictionary and List Schemas, as well as their respective Variants.</p>"},{"location":"API/#build","title":"<code>:Build()</code>","text":"<p>Dictionary &amp; List Schema Syntax: <pre><code>Schema:Build(): Schema\n</code></pre></p> <p>Precomputes and compiles the internal logical structures of the schema and all its configured variants to optimize runtime serialization and deserialization pipelines.</p> <p>Performance Recommendation: Always Call <code>:Build()</code> Post-Configuration</p> <p>It is highly recommended to invoke <code>:Build()</code> immediately after defining or modifying your schema structure (e.g., during initialization/startup). Pre-compiling layout logic prevents runtime overhead. If this method is omitted, the underlying layout structures will be computed dynamically on-demand during the very first serialization or deserialization operation, introducing an undesirable performance penalty on that initial payload cycle.</p> <p>Critical Runtime Warning: Schema Mutations After Instantiation</p> <p>Modifying the layout or structure of a schema at runtime after initialization is highly discouraged. If structural adjustments are absolutely necessary during runtime, you must manually invoke <code>:Build()</code> immediately following the modification. </p> <p>Failure to rebuild the schema while the system relies on an internally cached logical structure will cause critical misalignments between memory allocations and active data properties, resulting in severe data corruption or unexpected deserialization failures.</p> <ul> <li>Ahead-of-Time Compilation: Evaluates all active field definitions, type allocations, and structural constraints across the entire variant history, baking them into ready-to-use structural tables.</li> <li>Idempotent Execution: Can be called safely at any point in the lifecycle; re-building updates the underlying cache structure to mirror the newest schema changes without resetting state.</li> </ul> <p>Returns <code>Schema</code>: Returns the current schema instance (<code>self</code>), allowing for clean method chaining during initialization.</p>"},{"location":"API/#load","title":"<code>:Load()</code>","text":"<p>Dictionary Schema Syntax: <pre><code>Schema:Load(data: buffer | DictData, options: Versioning?): DictData\n</code></pre></p> <p>List Schema Syntax: <pre><code>Schema:Load(data: buffer | ListData, options: Versioning?): ListData\n</code></pre></p> <p>Parses, sanitizes, and normalizes the provided data structure according to the target schema or variant configuration.</p> <ul> <li>Input Mutability: Accepts both raw serialized <code>buffer</code> formats and unencoded data tables (<code>DictData</code> / <code>ListData</code>).</li> <li>Version Interoperability: Automatically detects and upgrades the payload structure regardless of legacy data versions, unless specific migration rules are overridden in the configuration options.</li> </ul> <p>Parameters</p> <ul> <li><code>data</code> (buffer | DictData | ListData): The raw buffer payload or data table to be processed.</li> <li><code>options</code> (Versioning, optional): Configuration settings governing output data structure versioning. Defaults to the latest available variant if omitted.</li> </ul> <p>Returns <code>(DictData | ListData?)</code>: The fully parsed, sanitized, and normalized data structure.</p>"},{"location":"API/#serialize","title":"<code>:Serialize()</code>","text":"<p>Dictionary Schema Syntax: <pre><code>Schema:Serialize(data: DictData, options: Serialization?): buffer\n</code></pre></p> <p>List Schema Syntax: <pre><code>Schema:Serialize(data: ListData, options: Serialization?): buffer\n</code></pre></p> <p>Encodes and packs the provided runtime data structure into a highly optimized binary stream. The resulting layout dynamically adapts to the selected serialization engine, structure layout mode, and compression algorithms, embedding all metadata necessary for fault-tolerant deserialization directly within the payload.</p> <ul> <li>Target Variant Selection: By default, compiles the data structural layout using the highest available schema variant version to ensure forward-compatible writing.</li> <li>Engine Optimization: Encodes the payload using the highest supported engine tier by default, ensuring maximum performance and optimal data compression ratios.</li> </ul> <p>Parameters</p> <ul> <li><code>data</code> (DictData | ListData): The unencoded data table or object structure matching the schema to be serialized.</li> <li><code>options</code> (Serialization, optional): Configuration settings governing serialization targets, such as overriding the default target variant version, mode, compression or engine configuration.</li> </ul> <p>Returns <code>buffer</code>: A compact, immutable binary stream ready for network transmission or persistent disk storage.</p>"},{"location":"API/#deserialize","title":"<code>:Deserialize()</code>","text":"<p>Dictionary Schema Syntax: <pre><code>Schema:Deserialize(data: buffer): (DictData, number)\n</code></pre></p> <p>List Schema Syntax: <pre><code>Schema:Deserialize(data: buffer): (ListData, number)\n</code></pre></p> <p>Decodes a raw binary stream back into its runtime table representation exactly as it was written, without applying any automated data migration or structural upgrading, sanitization is applied anyway.</p> <p>Usage Recommendation: Prefer <code>:Load()</code></p> <p>For standard workflow scenarios, it is highly recommended to use <code>:Load()</code> instead of <code>:Deserialize()</code>. While <code>:Load()</code> automatically handles version upgrades and structural normalization to the latest schema variant, <code>:Deserialize()</code> strictly outputs the data in the exact historical version it was originally serialized to.</p> <ul> <li>Raw Extraction: Directly unpacks the data structure relative to its embedded engine configuration and layout mode, completely bypassing the schema's migration pipeline.</li> </ul> <p>Parameters</p> <ul> <li><code>data</code> (buffer): The compact binary stream to be deserialized.</li> </ul> <p>Returns</p> <ul> <li><code>(DictData | ListData)</code>: The decoded data structure at its native, serialized variant version.</li> <li><code>number</code>: The versioning of the variant of the schema in question.</li> </ul>"},{"location":"API/#variant","title":"<code>:Variant()</code>","text":"<p>Dictionary &amp; List Schema Syntax: <pre><code>Schema:Variant(version: number?): Variant\n</code></pre></p> <p>Instantiates a new schema variant, allowing for backward-compatible structural iterations, incremental property additions, or type overrides on an existing schema configuration.</p> <p>Version Assignment: Sequential vs. Explicit</p> <p>If the <code>version</code> parameter is omitted, the framework automatically assigns an incremental version index at runtime based on the order in which the variants are declared. For strict production tracking, you can explicitly pass a custom version number to freeze and isolate its historical identification.</p> <ul> <li>Incremental Evolution: Provides a clean boundary to append new properties or modify existing schema data fields without corrupting the historical parsing layout of older versions.</li> <li>Inheritance &amp; Overwriting: Inherits all definitions from the base schema configuration while allowing specific fields to be safely overwritten or adapted to accommodate evolving payload designs.</li> </ul> <p>Parameters</p> <ul> <li><code>version</code> (number, optional): An explicit identifier for this specific schema generation. If omitted, the system defaults to sequential ordered assignment at runtime.</li> </ul> <p>Returns <code>Variant</code>: Returns the newly instantiated variant instance, allowing for fluent method chaining to build out the new variant's unique structural layout.</p>"},{"location":"API/#template","title":"<code>:Template()</code>","text":"<p>Dictionary Schema Syntax: <pre><code>Schema:Template(options: Versioning?, lists: boolean?): DictData\n</code></pre></p> <p>List Schema Syntax: <pre><code>Schema:Template(options: Versioning?, lists: boolean?): ListData\n</code></pre></p> <p>Generates a fully initialized boilerplate data table pre-populated with default values and conforming precisely to the specified schema variant configuration.</p> <p>List Behavior: The <code>lists</code> Parameter</p> <p>Passing <code>true</code> for the optional <code>lists</code> parameter guarantees that every nested or top-level list structural block within the generated template will be populated with at least one default-initialized item. If omitted or <code>false</code>, arrays and sequences default to an empty structural layout (<code>{}</code>).</p> <ul> <li>Sanitized Baseline: Every data field is initialized using the default parameter values defined in your schema, running them through the framework's internal normalization layout to ensure structural integrity out of the box.</li> <li>Target Version Mocking: Resolves structural properties relative to the configuration provided in the <code>options</code> parameter, making it easy to generate templates mirroring specific legacy or forward schema variations.</li> </ul> <p>Parameters</p> <ul> <li><code>options</code> (Versioning, optional): Configuration settings pointing to the specific schema variant target you want to generate a mockup for.</li> <li><code>lists</code> (boolean, optional): Instructs the template generator to enforce a minimum length of one item inside every array block when set to <code>true</code>.</li> </ul> <p>Returns <code>DictData | ListData</code>: A completely fresh, sanitized runtime data table prepared for application state mutation or sandbox initialization.</p>"},{"location":"API/#logic","title":"<code>:Logic()</code>","text":"<p>Dictionary Schema Syntax: <pre><code>Schema:Logic(options: Versioning?): SchematizedDict\n</code></pre></p> <p>List Schema Syntax: <pre><code>Schema:Logic(options: Versioning?): SchematizedList\n</code></pre></p> <p>Exposes and extracts the underlying logical layout map of the schema compiled for the targeted structural variant version.</p> <p>Under the Hood: Direct Layout Reflection</p> <p>The data structure returned by this method is the exact internal logical mapping that <code>:Build()</code> generates under the hood. While <code>:Build()</code> caches these configurations internally to accelerate runtime serialization, <code>:Logic()</code> acts as a mirror, exposing that parsed architectural blueprint directly to your scripts.</p> <ul> <li>Architectural Insights: Reflects the fully processed mapping of fields, making it highly valuable for custom debugging tools, introspection, or automated schema analysis.</li> <li>Versioned Resolution: Automatically evaluates the data structures relative to the rules passed via the <code>options</code> parameter. If omitted, it defaults to evaluating the highest (latest) available schema variant.</li> </ul> <p>Parameters</p> <ul> <li><code>options</code> (Versioning, optional): Configuration constraints specifying which historical or target variant generation should have its logical map resolved.</li> </ul> <p>Returns <code>SchematizedDict | SchematizedList</code>: The internal structural blueprint mapping all fields, constraints, and operational layouts for the evaluated schema iteration.</p>"},{"location":"API/#bytes","title":"<code>:Bytes()</code>","text":"<p>Dictionary Schema Syntax: <pre><code>Schema:Bytes(data: DictData, options: Serialization?): number\n</code></pre></p> <p>List Schema Syntax: <pre><code>Schema:Bytes(data: ListData, options: Serialization?): number\n</code></pre></p> <p>Calculates and returns the exact buffer payload size required to serialize the provided runtime data structure.</p> <p>Important Note: Roblox Buffer Compression Exclusion</p> <p>The byte count returned by this method represents the uncompressed size calculated according to the internal serialization engine. It does not account for roblox buffer compression layer.</p> <ul> <li>Layout Profiling: Simulates structural layout packing relative to the selected serialization engine, configuration mode, and active schema variant to compute precise byte footprints.</li> <li>Deterministic Metrics: Accounting for internal variable-length encodings or structural overhead, it determines the true, absolute memory byte budget required.</li> </ul> <p>Parameters</p> <ul> <li><code>data</code> (DictData | ListData): The unencoded data table or object structure matching the schema to measure.</li> <li><code>options</code> (Serialization, optional): Configuration overrides for targeted engine versions, modes, or specific schema variant environments.</li> </ul> <p>Returns <code>number</code>: The exact total number of bytes the payload will consume upon serialization.</p>"},{"location":"API/#data-type","title":"<code>:Data-Type()</code>","text":"<p>The building blocks used to define schema configurations. Every type method acts as a builder utility, extending the active variant's layout layout definitions.</p> <p>Critical Structure Warning: Method Execution Order</p> <p>The precise sequential order in which schema-building methods are invoked directly determines the underlying buffer allocation layout. Arbitrarily altering this execution order will break layout alignment, resulting in data corruption or deserialization failures when loading existing persistent data stores.</p> <p>Depending on your active serialization mode, you can safely append new properties to the end of the schema or within individual dictionaries without creating a new variant. However, structural modifications must be handled with extreme care to preserve data integrity.</p> <p>Allocation Baseline: Minimum Sizing Defaults</p> <p>By default, all primitive, numeric, and scalar types initialize and build their layouts starting from their lowest possible allocable byte size. </p> <p>If your application needs to handle larger numeric ranges, longer structural strings, or higher-capacity byte boundaries, you must explicitly define and configure these limits inside the type's optional configuration block (such as specifying parameters within Entry, LEntry, or EEntry). Leaving options omitted locks the property to its tightest, most memory-optimized footprint.</p> <p>Syntax Variation</p> <ul> <li><code>DictVariant</code> Methods: Require an explicit <code>key: string</code> identifier and return <code>DictVariant</code> (<code>self</code>) to allow method chaining.</li> <li><code>ListVariant</code> Methods: Direct value allocations within arrays; they do not accept a key parameter and return <code>()</code> (void).</li> </ul>"},{"location":"API/#structural-nested-types","title":"Structural &amp; Nested Types","text":"<p>Used to nest other schemas or create complex structural layouts.</p> Method <code>DictVariant</code> Syntax <code>ListVariant</code> Syntax Description <code>schema</code> <code>:schema(key, schema, options?)</code> <code>:schema(schema, options?)</code> Embeds an external <code>Schema</code> or <code>Variant</code> definition directly into the current layout. Serialization options are not inherited from the current schema. <code>dict</code> <code>:dict(key, callback)</code> <code>:dict(callback)</code> Creates an inline sub-dictionary, passing a temporary <code>DictVariant</code> to the callback configuration function. <code>list</code> <code>:list(key, callback)</code> <code>:list(callback)</code> Creates an inline sub-list array, passing a temporary <code>ListVariant</code> to the callback configuration function. <p>Important Layout Warning: Serialization Options Are Not Inherited</p> <p>When utilizing the <code>:schema()</code> type builder to embed external configurations, please note that any Serialization <code>options</code> (such as engine versions, layout modes, or specific variant configurations) assigned to the parent schema or current variant are not implicitly inherited by the child schema.</p> <p>If your serialization strategies depend on explicit parameters (e.g., maintaining full persistence, partial data store persistence, or specialized structural tracking), you must provide those options directly inside the <code>:schema()</code> configuration call. Failing to pass these overrides will result in the nested schema falling back to its default runtime engine layout.</p>"},{"location":"API/#primitive-common-types","title":"Primitive &amp; Common Types","text":"<p>Standard primitive values, allocations, and dynamic utilities.</p> Method <code>DictVariant</code> Call Syntax <code>ListVariant</code> Call Syntax Configuration Reference Description <code>bool</code> <code>:bool(key, options?)</code> <code>:bool(options?)</code> Entry<code>Size: BYTE</code> Standard true/false boolean payload. <code>bits</code> <code>:bits(key, options?)</code> <code>:bits(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Bitmask layout configuration, boolean array <code>char</code> <code>:char(key, options)</code> <code>:char(options)</code> LEntry<code>Size: BYTE</code> Fixed-length text value string layout. <code>varchar</code> <code>:varchar(key, options?)</code> <code>:varchar(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Variable-length text value string serialization. <code>buffer</code> <code>:buffer(key, options?)</code> <code>:buffer(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Raw data buffer payload stream embedded directly into the schema. <code>enum</code> <code>:enum(key, options)</code> <code>:enum(options)</code> EEntry<code>Size: BYTE | WORD | DWORD</code> Maps an enumerated value, EnumItem, string or number. <code>id</code> <code>:id(key, options?)</code> <code>:id(options?)</code> Entry<code>Size: QWORD</code> A unique identifier tracking reference value, comparable to a positive float64. <code>auto</code> <code>:auto(key, options?)</code> <code>:auto(options?)</code> Entry Dynamic on-demand inference mapping engine based on incoming value types."},{"location":"API/#numeric-scalars","title":"Numeric Scalars","text":"<p>Basic single-value math formats.</p> Method <code>DictVariant</code> Call Syntax <code>ListVariant</code> Call Syntax Configuration Reference Description <code>number</code> <code>:number(key, options?)</code> <code>:number(options?)</code> Entry<code>Size: DWORD | QWORD</code> Standard runtime floating-point or generic numeric scaling allocation. Allows nan and inf. <code>float</code> <code>:float(key, options?)</code> <code>:float(options?)</code> Entry<code>Size: DWORD | QWORD</code> Floating point binary layout. <code>int</code> <code>:int(key, options?)</code> <code>:int(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Signed runtime integer scalar format. <code>uint</code> <code>:uint(key, options?)</code> <code>:uint(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Unsigned runtime integer scalar layout format."},{"location":"API/#vector-classes","title":"Vector Classes","text":"<p>Optimized structural dimensions grouped in spatial coordinates.</p> Method <code>DictVariant</code> Call Syntax <code>ListVariant</code> Call Syntax Configuration Reference Description <code>numberVec2</code> <code>:numberVec2(key, options?)</code> <code>:numberVec2(options?)</code> Entry<code>Size: DWORD | QWORD</code> Vector2 data in number. <code>floatVec2</code> <code>:floatVec2(key, options?)</code> <code>:floatVec2(options?)</code> Entry<code>Size: DWORD | QWORD</code> Vector2 data in float. <code>intVec2</code> <code>:intVec2(key, options?)</code> <code>:intVec2(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Vector2 data in int. <code>uintVec2</code> <code>:uintVec2(key, options?)</code> <code>:uintVec2(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Vector2 data in uint. <code>numberVec3</code> <code>:numberVec3(key, options?)</code> <code>:numberVec3(options?)</code> Entry<code>Size: DWORD | QWORD</code> Vector3 data in number <code>floatVec3</code> <code>:floatVec3(key, options?)</code> <code>:floatVec3(options?)</code> Entry<code>Size: DWORD | QWORD</code> Vector3 data in float <code>intVec3</code> <code>:intVec3(key, options?)</code> <code>:intVec3(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Vector3 data in int <code>uintVec3</code> <code>:uintVec3(key, options?)</code> <code>:uintVec3(options?)</code> Entry<code>Size: BYTE | WORD | DWORD</code> Vector3 data in uint"},{"location":"API/#roblox-structural-datatypes","title":"Roblox Structural Datatypes","text":"<p>Specialized data coordinate systems used exclusively within the Roblox platform environment.</p> Method <code>DictVariant</code> Call Syntax <code>ListVariant</code> Call Syntax Configuration Reference Description <code>brickcolor</code> <code>:brickcolor(key, options?)</code> <code>:brickcolor(options?)</code> Entry<code>Size: WORD</code> Serializes a Roblox <code>BrickColor</code> asset token cleanly into memory. <code>color3</code> <code>:color3(key, options?)</code> <code>:color3(options?)</code> Entry<code>Size: BYTE</code> Standard Roblox <code>Color3</code> RGB color representation format. <code>cframe</code> <code>:cframe(key, options?)</code> <code>:cframe(options?)</code> Entry<code>Size: DWORD | QWORD</code> Full allocation for <code>CFrame</code>. <code>lcframe</code> <code>:lcframe(key, options?)</code> <code>:lcframe(options?)</code> Entry<code>Size: DWORD | QWORD</code> Light-weight variant for <code>CFrame</code>."},{"location":"API/#options-objects","title":"Options Objects","text":""},{"location":"API/#versioning","title":"<code>Versioning</code>","text":"<p>The <code>Versioning</code> configuration object controls historical data resolution, migration paths, and decoding behaviors during deserialization operations or mockup generation.</p> <pre><code>export type Versioning = {\n    Variant: number?,\n    Engine: number?\n}\n</code></pre> <p>Default Engine Fallback Behavior</p> <p>If either field is omitted, the framework automatically targets the highest available version definitions (i.e., the latest schema variant iteration and the highest supported internal processing engine tier).</p> <p>Fields</p> <p><code>Variant</code> (number, optional) Explicitly targets a specific generation or variant iteration of the schema layout. </p> <ul> <li>When defined, forces data operations (like parsing or template mocking) to match that exact historical layout version.</li> <li>When omitted, defaults to the latest variant version registered on the schema.</li> </ul> <p><code>Engine</code> (number, optional) Overrides the default internal serialization/deserialization engine version tier.</p> <ul> <li>Allows you to safely decode binary payloads packed with legacy engine configurations or restrict dynamic parsing blocks to a specific engine level.</li> <li>When omitted, defaults to the newest and highest performance engine tier available within the framework environment.</li> </ul>"},{"location":"API/#serialization","title":"<code>Serialization</code>","text":"<p>The <code>Serialization</code> configuration object dictates explicit layout compilation, structural format modes, and performance optimization flags applied during the binary serialization process.</p> <pre><code>export type Serialization = {\n    Compress: Compression?,\n    Variant: number?,\n    Engine: number?,\n    Mode: Mode?\n}\n</code></pre> <p>Inherited Behavior &amp; Fallbacks</p> <p>The <code>Variant</code> and <code>Engine</code> fields behave identically to those defined in the <code>Versioning</code> type block. If any optional configuration parameter is omitted, the framework automatically targets the highest available production defaults (latest variant version, highest engine tier, flat packing mode, and uncompressed stream profiles).</p> <p>Fields</p> <p><code>Mode</code> (string, optional) Configures the physical memory layout alignment structure of the resulting binary buffer. Currently supports the following structural layouts (with expansion support planned for future modes):</p> <ul> <li><code>Flat</code> (Default): Packs all data inline, restricting subsequent structural modifications to the very end of the buffer. This maximizes encoding efficiency and minimizes footprint, making it ideal for high-performance, ephemeral runtime data such as remote network events.</li> <li><code>Nested</code>: Isolates individual dictionaries with distinct structural boundaries, allocating space for new properties to be appended to any nested object layer. This layout is highly optimized for persistent data stores that continuously evolve over time.</li> </ul> <p><code>Compress</code> (Compression, optional) Defines the downstream compression strategy or threshold layer applied to the binary stream.</p> <p><code>Variant</code> (number, optional) from <code>Versioning</code>.</p> <p><code>Engine</code> (number, optional) from <code>Versioning</code>.</p>"},{"location":"API/#entries","title":"<code>Entries</code>","text":"<p>These type configurations define how primitive properties, string layouts, and enumeration indices allocate memory boundaries and sanitize incoming payload data.</p>"},{"location":"API/#entry","title":"<code>Entry</code>","text":"<p>The primary baseline configuration tracking numeric ranges, sanitization, scaling boundaries, and precision adjustments.</p> <pre><code>export type EntryOptions = {\n    Sanitizers: Sanitizers?,\n    Round: number?,\n    Default: any?,\n    Size: Sizes?\n}\n</code></pre> <p>Fields</p> <p><code>Sanitizers</code> (Sanitizers, optional): A processing pipeline invoked to clean or adjust the incoming value.</p> <p><code>Round</code> (number, optional): Clamps values or targets specific floating-point decimal precision boundaries before storage operations.</p> <p><code>Default</code> (any, optional): The initial or placeholder value assigned if no runtime input is supplied.</p> <p><code>Size</code> (Sizes, optional): Dictates the primitive bit-budget footprint. Defaults to the minimum allocable layer if omitted.</p>"},{"location":"API/#lentry-length-options","title":"<code>LEntry</code> (Length Options)","text":"<p>Configures explicit, fixed-length allocation structures\u2014primarily utilized by strict char array boundaries (<code>:char</code>).</p> <p>Fields</p> <p><code>Fields</code> inherited from Entry</p> <p><code>Lenght</code> (number): Enforces an explicit, fixed allocation boundary (e.g., number of sequential string characters allocated directly to the byte payload structure).</p>"},{"location":"API/#enumoptions","title":"<code>EnumOptions</code>","text":"<p>Configures categorical sets, indexing references, or Roblox Enum arrays to pack integer identifiers efficiently.</p> <p>Critical Persistence Rule: Appending to Literal String Arrays</p> <p>When configuring <code>Values</code> using a literal array of strings (<code>{string}</code>), the serialization engine internally maps each entry to its sequential integer index. </p> <p>To maintain data compatibility and persistence over time, you must only append new strings to the absolute bottom (end) of the array. Modifying the order, deleting items, or inserting elements anywhere else will shift the internal index mappings, causing existing saved data to point to the wrong string values.</p> <p>Fields</p> <p><code>Fields</code> inherited from Entry</p> <p><code>Values</code> (<code>Enum | {string}</code>): The explicit master lookup definitions list (such as a Roblox native <code>Enum</code> category or an array of literal state strings).</p>"},{"location":"API/#core-definition-blocks","title":"Core Definition Blocks","text":"<p>The fundamental baseline types supporting the options blocks detailed above.</p>"},{"location":"API/#compression","title":"<code>Compression</code>","text":"<p>The <code>Compression</code> configuration object defines post-processing compression rules applied immediately before finalizing a binary payload buffer, wrapper-enforcing Roblox's internal buffer compression algorithms via EncodingService.</p> <pre><code>export type Compression = {\n    Type: Enum.CompressionAlgorithm,\n    Level: number?\n}\n</code></pre> <p>Execution Lifecycle: End-of-Stream Pipeline</p> <p>Compression routines act as an automated final layer wrapped cleanly around the data lifecycle. The algorithm is invoked at the absolute end of the serialization pipeline (after all structures, options, and variants have been packed) and executes automatically at the absolute start of the deserialization process before unpacking any data.</p> <p>Fields</p> <p><code>Type</code> (Enum.CompressionAlgorithm) Specifies the core algorithm used to compress the serialized buffer. This maps directly to Roblox's native compression options. Defaults with <code>Enum.CompressionAlgorithm.Zstd</code>.</p> <p><code>Level</code> (number, optional) Sets the algorithmic compression intensity/effort tier. </p> <ul> <li>Higher levels produce smaller byte sizes at the cost of increased CPU compression overhead.</li> <li>Lower levels prioritize execution speed and minimize processing latency.</li> <li>If omitted, the framework defaults to the native default compression level specified by Roblox's EncodingService.</li> </ul>"},{"location":"API/#sanitizers","title":"<code>Sanitizers</code>","text":"<p><pre><code>export type Sanitizers = { ((value: any, type: string) -&gt; any?) | Sanitizers }\n</code></pre> An array mapping processing functions or recursive nested groups. Each sanitizer function accepts the current runtime <code>value</code> along with its layout <code>type</code> identifier string, and can return a newly mutated or sanitized variant value to proceed down the serialization chain. You can also assert values, if nil is returned it will attempt to set the default value, if present.</p>"},{"location":"API/#sizes","title":"<code>Sizes</code>","text":"<pre><code>type Sizes = \"BYTE\" | \"WORD\" | \"DWORD\" | \"QWORD\"\n</code></pre> <p>When we talk about objects with multiple values (<code>:cframe</code>, <code>:intVec3</code>, ...), the size is intended to be the allocated space for each one of these, or the max ammount of values that the type can handle (<code>:enum</code>, <code>:char</code>)</p> <p>Defines the absolute byte limits assigned to structural memory fields:</p> <ul> <li><code>\"BYTE\"</code>: 8-bit type allocation.</li> <li><code>\"WORD\"</code>: 16-bit type allocation.</li> <li><code>\"DWORD\"</code>: 32-bit type allocation (Double Word).</li> <li><code>\"QWORD\"</code>: 64-bit maximum type allocation (Quad Word).</li> </ul>"},{"location":"Samples/","title":"Samples","text":""},{"location":"Samples/#definitions-and-usages","title":"Definitions and Usages","text":"<p>Let's gradually define a Player's data as an approach on how to use the framework. This is not an actual usage of player data</p> <p>We'll define a dictionray with Username and UserId for our Player. <pre><code>local dict = PackIt.dict(\"Player\")\n\ndict:varchar(\"Username\") -- BYTE Lenght string\n    :id(\"UserId\") -- Positive float64\n\ndict:Build()\n</code></pre></p> <p>We now want to make the player own a list of items, so we are going to define a list of items ids inside it. Since we are appending to the bottom of the dictionary we dont have to make a new variant. Works for nested dicts aswell with Nested serialization mode.</p> <pre><code>dict:list(\"Items\", function(list)\n    list:id() -- {Positive float64}\n\nend):Build()\n</code></pre> <p>Now we want to associate a price to each item (max ~42 mil and defaults to 0, can have 2 digit decimals), we want to change old list to include it without corrupting previous data, so we are going to use versioning. (Migration must be handled somewhere in this case)</p> <pre><code>local variant = dict:Variant()\n\nvariant:list(\"Items\", function(list)\n    list:dict(function(dict)\n        dict:uint(\"Price\", {\n            Size = \"DWORD\", -- Max 4.294.967.296\n            Default = 0, -- Defaults to 0\n            Round = .01 -- Store decimals into uint\n\n        }):id(\"ItemId\")\n    end)\nend)\n\nvariant:Build()\n</code></pre> <p>Now we want to have a list of players. This example doesnt really need nested mode as there is no nested dictionary in another dictionary yet.</p> <pre><code>local options = {\n    Mode = \"Nested\" -- \"Flat\" by default\n    -- Do not use Compress in sub-schemas\n    -- Variant = 1 (Highest by default)\n    -- Engine = 1 (Highest by default)\n}\n\nlocal list = PackIt.list(\"Players\")\nlist:schema(dict, options) -- Options are not inherited form list to variant\nlist:Build() -- dict is logically equivalent to variant\n</code></pre> <p>Lets now Serialize and Load the data.</p> <pre><code>local options = {\n    Compress = { Type = Enum.CompressionAlgorithm.Zstd },\n    Mode = \"Nested\"\n}\n\nlocal versioning = {\n    Variant = 1 -- Highest by default\n}\n\nlocal template = list:Template(versioning) -- Generates mock data\nlocal buffer = list:Serialize(template, options) -- Still not inherited in sub-schemas\nlocal data = list:Load(buffer, versioning) -- Deserialized data\n</code></pre>"},{"location":"Samples/#conclusions","title":"Conclusions","text":"<p>This schema resulted ~60% smaller than json in flat mode on 10 users with 1-5 items each</p>"},{"location":"Usages/","title":"Usages","text":""},{"location":"Usages/#optimize-networking","title":"Optimize Networking","text":"<p>When sending data across the server-client boundary via <code>RemoteEvents</code> or <code>RemoteFunctions</code>, your primary goal is to minimize network bandwidth and completely eliminate serialization overhead.</p> <ul> <li>How it works: By using a Shared Schema (accessible by both Server and Client) paired with <code>\"Flat\"</code> mode, the framework linearizes your data structures directly into a continuous binary stream, dropping table overhead and string keys entirely.</li> <li>The Performance Edge: Layering <code>Compress</code> options on top of this flat stream squeezes the payload down to its absolute minimum size. Because the layout is already flat and predictable, Roblox\u2019s underlying <code>EncodingService</code> can compress the buffer with incredibly low CPU overhead, resulting in blistering-fast execution speeds and minimal packet sizes over the wire.</li> <li>Secure Inbound Validation: Beyond sheer performance, you can use <code>Sanitizers</code> directly inside your shared schema options to act as automated security checkpoints. When a client triggers a <code>RemoteFunction</code> or <code>RemoteEvent</code>, the server automatically routes the incoming unpacked data through your sanitization pipeline first. This allows you to clamp unexpected numbers, enforce maximum boundary lengths on user-submitted text, or catch malicious payloads seamlessly during the deserialization phase before the data ever reaches your core game logic.</li> <li>Built-in Type Safeguards: The engine features native, hardcoded type-validation guardrails at the lowest level. For example, when using a numeric type which is not <code>number</code> (int, uint, float, ...), the engine actively blocks illegal or broken numbers like <code>NaN</code> (Not a Number) or <code>inf</code> (Infinity). It forces incoming data to conform strictly to the physics of that specific primitive boundary, catching malformed states and ensuring your code never has to deal with corrupt arithmetic.</li> </ul> <pre><code>local payload = PackIt.get(\"Schema\"):Serialize(data, options)\n-- Send over the network\nlocal data = PackIt.get(\"Schema\"):Load(payload)\n</code></pre>"},{"location":"Usages/#dynamic-remote-parameters","title":"Dynamic Remote Parameters","text":"<p>If you need a flexible way to pass arbitrary parameters through <code>RemoteEvents</code> or <code>RemoteFunctions</code> without declaring rigid, dedicated properties for every unique event, you can set up a single list schema populated by the <code>auto</code> type.</p> <ul> <li>How it works: You create a schema containing a simple list defined with the <code>:auto()</code> type builder. When you pass a sequence of parameters through this list, the engine automatically checks the type signatures at runtime.</li> <li>Universal Remote Payload: As long as the data types you pass are supported by the framework (such as primitives, numbers, vectors, or CFrames), you can serialize any collection of parameters into a tiny binary buffer. This gives you the ultimate structural freedom of an untyped network pipeline, combined with the extreme performance and packet shrinkage of a custom binary layout.</li> </ul> <pre><code>PackIt.list(\"Schema\"):auto()\n</code></pre>"},{"location":"Usages/#optimize-cloud-storage-with-flexibility","title":"Optimize Cloud Storage with Flexibility","text":"<p>When saving game data to Roblox DataStores or frameworks like ProfileService, your primary challenge isn\u2019t network transit\u2014it is long-term flexibility and the ability to safely update your data structures over time.</p> <ul> <li>How it works: Roblox DataStores natively support saving raw binary buffers. By saving your data schemas using <code>\"Nested\"</code> mode, the engine isolates individual dictionaries with distinct structural boundaries, allocating space for new properties to be appended to any nested object layer. This layout is highly optimized for persistent data stores that continuously evolve over time.</li> <li>The Storage Edge: This layout makes it incredibly easy to manage migrations, schema mutations, and backwards-compatible changes across different player versions. As your game expands and features are added, the nested layout ensures old profile data cleanly upgrades without breaking existing save states.</li> <li>Compression Savings: Just like with flat mode, you can layer <code>Compress</code> options on top of your nested layout. This allows you to achieve massive storage optimizations\u2014reducing DataStore footprints by up to 90%+ compared to json while preserving full structural upgrade flexibility.</li> </ul>"}]}